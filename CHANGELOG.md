# 变更记录

本文件记录对外发布的版本变化。仓库只发布**部署与管理方案**，
标准正文不属于发布内容，详见 README 的「内容边界」。

## v0.2.0 — 2026-09-19

在 v0.1.0 的方案之上补上**一个能直接用的查询成品**，并把知识库本身做完：
正文库导入完成、目录库全量重建、候选标准的官方状态核验落地。

### 新增

- **查询应用 `ragflow-query/`**：双击 `启动查询.bat` 即可使用的本地查询界面。
  后端 `app.py` 单文件、**只用 Python 标准库**（无需 `pip install`），
  API Token 自动从 MySQL 读取；附 `accept_query.py` 一键端到端验收。
  设计要点：助手关闭引用面板（`quote=false`）使 `chat/completions` 的 `reference`
  恒为空，故引用改走 `/api/retrieval`、回答走 `/api/chat`，前端**并行取**——
  引用约 0.5 s 先出、回答约 30 s 后到。内置上游抖动退避重试。
- **导入与重建工具**：
  - `import_texts_to_ragflow.py`：正文批量导入，**批内失败自动单独重试**（幂等可重跑）。
  - `rebuild_catalog.py`：目录库重建，**先清后传**——RAGFlow 对同名文件不覆盖，
    直接重传会存成 `xx(1).md`，使同一标准出现两条记录。
  - `verify_knowledge_base.py`：库存核对 + 检索探针 + 端到端问答，一条命令产出报告。
- **元数据与核验**：`06-标准总目录候选.jsonl` 增至 **78 条**，其中 **14 条**查证了
  官方状态、发布/实施日期、替代关系与来源 URL（新增 `17-AC标准状态核验.md`）。
  新增 `GA/T 2160-2024`（现行）与 `GA/T 825-2009`（**已废止**，废止于 2015-04-19）。
- **记录**：`16-验收报告.md`（库存 + 检索 + 端到端）、`18-目录库重建记录.md`。

### 变更

- **验收报告默认脱敏。** 命中的**条款原文**与含条款原文的**回答正文**不再写入报告，
  只保留标准编号、条款号、相似度与判定结果——能证明「命中了正确标准与正确条款」的
  正是编号与条款号本身。需要含原文的版本时加 `--with-clause-text`，该版本**只留本地**。
- 目录库 **85 张 → 87 张**卡片，全部为核验后的状态；正文库 **15 份 / 424 切片**。

### 修正

- **更正先前的结论：embedding `RemoteDisconnected` 不必重启容器。**
  对同一问题连续重发 3 次**全部成功**，且容器运行时长未变——此前「必须
  `docker restart docker-ragflow-cpu-1`」的判断，是把「重试恰好成功」误归因给了重启。
  正确做法是**应用层退避重试**，`docker restart` 只作最后手段。
- **`start-docker.ps1` 找不到 Docker Desktop。** 脚本只认
  `%LOCALAPPDATA%\Programs\DockerDesktop\Docker Desktop.exe`（按用户安装的位置），
  而 Docker Desktop 也可能装在 `C:\Program Files\Docker\Docker\`（按机器安装）；
  后者会在启动前直接抛错，恢复流程根本走不到。改为按候选列表探测两种布局。
- **批量解析的失败统计偏小。** 轮询函数一遇到失败文档就返回，
  同批仍在解析的文档被漏计（曾把 86 份报成 38 份）。改为等整批到终态再返回。
- **补上悬空引用。** 14 张卡片与 `06-标准总目录候选.jsonl` 都引用了
  `17-AC标准状态核验.md`，而该文件此前并不存在，现已按核验数据生成。

### 已验证的能力

- 查询应用端到端：6 条用例，引用 6/6、回答 6/6；无关问题 **0 引用 + 短拒答**，不编造。
- 知识库端到端：库存核对、检索探针、问答用例 3/3。
- 目录库检索：87 个切片全部可被命中，**无孤儿切片**（删旧文档后检索不会返回已删内容）。

### 已知限制

- 候选标准多数（**64/78**）仍为「待权威核验」，按验收规则不得作为主推荐，
  而 AC-01～AC-12 的核心判定项正是主推荐，因此这套用例仍不能全部按设计判定。
  这是数据核验问题，不是检索问题。
- 目录库卡片的「正文状态」仍标「未获取」，而 15 份标准已进入正文库，与实际不符。
- 场景匹配只做了 6 个抽样用例，未跑完整 24 个。
- 条款级结论只在转换质量合格的文件上成立。
- 本机 Docker Desktop / WSL2 不稳定（本版期间出现过一次引擎整体掉线，
  由 `start-docker.ps1` 恢复）。

### 本版本踩到并已记录的问题

1. 助手 `prompt_config.quote=false` 会让 `chat/completions` 的 `reference` 恒空。
2. `/api/v1/chat/completions` 只认 `chat_id`，不认 OpenAI 风格的 `model`——
   传错会静默回退到无知识库、无重排的默认对话，引用恒空且单次耗时涨到 99～263 s。
3. `/api/v1/retrieval` 的 `page_size` **不能超过库内切片总数**，否则报 `code=102`。
4. RAGFlow 对同名文件不覆盖，重复上传会产生 `xx(1).md` 副本。
5. `knowledgebase.chunk_num` 是增量维护的计数器，删文档后偏大；
   核对库存要以 `document` 表为准。
6. 并发解析每批必挂 1 份，单发重试必过（embedding 端限流，与文本内容无关）。
7. embedding 偶发抖动靠应用层退避重试即可，不必重启服务。

## v0.1.0 — 2026-09-15

首个打包版本。交付的是可直接照着搭起来的一套方案，而不是成品知识库。

### 包含

- **部署配置**：官方 RAGFlow Compose 的本地覆盖，只把 Web 入口绑定到
  `127.0.0.1:8081`，数据库、搜索、对象存储和缓存不对外暴露端口。
- **运维脚本**：`compose.ps1`（启停，自动加载部署专用密码文件）与
  `start-docker.ps1`（引擎无响应时的恢复流程）。
- **导入工具**：元数据卡片生成、正文清洗、按阅读顺序重转、转换质量择优、
  零依赖资产校验。
- **标准元数据**：15 份已取得正文标准的详细卡片与哈希台账，
  76 条候选标准目录，以及据此生成的 85 张标准卡片索引。
- **文档**：整体方案书、场景分类与验收用例设计、检索与场景匹配验收记录、
  批量导入记录、按优先级排列的后续改进方案。

### 不包含

- 标准正文（原始 PDF、转换后的 Markdown、清洗版与重转版）。
- 切片关联明细，其中含条款原文。
- 任何部署凭据。

拿到本包的人需要自备标准正文，并自行完成来源与状态的核验。

### 已验证的能力

- 检索层：正确标准与具体条款可被精确召回（含 `第 5.2.2.1 条` 级别的命中）。
- 端到端问答：抽样四类问题均给出正确条款依据；证据不足类问题正确拒答。
- 引用合规：同一问题连续 5 次，均给出「标准编号 + 条款号」，无内部引用标记。
- 场景匹配：抽样 6 个用例，2 个通过、2 个部分通过、2 个未通过。

### 已知限制

- 候选标准全部处于「待权威核验」，按验收规则不得作为主推荐，
  因此场景匹配用例集尚不能按设计完整判定。
- 目录缺少 `GA/T 2160-2024`、`GA/T 825-2009`。
- 其余 13 份已取得正文尚未导入完毕。
- 本机 Docker Desktop / WSL2 环境不稳定，已通过固定容器 DNS、
  限制 Elasticsearch 堆内存与恢复脚本缓解。

### 本版本踩到并已记录的问题

1. 新建知识库时 RAPTOR 与 GraphRAG 默认开启，解析负载成倍增加。
2. Elasticsearch 未设堆上限时按容器内存自动放大到 4.4 GB。
3. 思考型生成模型在 RAGFlow 中会出现空回答。
4. RAGFlow 会追加自己的引用规则，覆盖助手提示词中的格式要求。
5. 按阅读顺序重转正文只在「正文被表格分割」的文件上有效，不是通用解。
6. 重建容器必须使用部署专用的密码文件，否则连不上 MySQL。
7. 正文库与目录库的相似度阈值需要分别标定。
