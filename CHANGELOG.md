# 变更记录

本文件记录对外发布的版本变化。仓库只发布**部署与管理方案**，
标准正文不属于发布内容，详见 README 的「内容边界」。

## v0.1.0 — 2026-09-15

首个打包版本。交付的是可直接照着搭起来的一套方案，而不是成品知识库。

### 包含

- **部署配置**：官方 RAGFlow Compose 的本地覆盖，只把 Web 入口绑定到
  `127.0.0.1:8081`，数据库、搜索、对象存储和缓存不对外暴露端口。
- **运维脚本**：`compose.ps1`（启停，自动加载部署专用密码文件）与
  `start-docker.ps1`（引擎无响应时的恢复流程）。
- **导入工具**：元数据卡片生成、正文清洗、按阅读顺序重转、转换质量择优、
  零依赖资产校验。
- **标准元数据**：15 份已取得正文标准的详细卡片与哈希台账，
  76 条候选标准目录，以及据此生成的 85 张标准卡片索引。
- **文档**：整体方案书、场景分类与验收用例设计、检索与场景匹配验收记录、
  批量导入记录、按优先级排列的后续改进方案。

### 不包含

- 标准正文（原始 PDF、转换后的 Markdown、清洗版与重转版）。
- 切片关联明细，其中含条款原文。
- 任何部署凭据。

拿到本包的人需要自备标准正文，并自行完成来源与状态的核验。

### 已验证的能力

- 检索层：正确标准与具体条款可被精确召回（含 `第 5.2.2.1 条` 级别的命中）。
- 端到端问答：抽样四类问题均给出正确条款依据；证据不足类问题正确拒答。
- 引用合规：同一问题连续 5 次，均给出「标准编号 + 条款号」，无内部引用标记。
- 场景匹配：抽样 6 个用例，2 个通过、2 个部分通过、2 个未通过。

### 已知限制

- 候选标准全部处于「待权威核验」，按验收规则不得作为主推荐，
  因此场景匹配用例集尚不能按设计完整判定。
- 目录缺少 `GA/T 2160-2024`、`GA/T 825-2009`。
- 其余 13 份已取得正文尚未导入完毕。
- 本机 Docker Desktop / WSL2 环境不稳定，已通过固定容器 DNS、
  限制 Elasticsearch 堆内存与恢复脚本缓解。

### 本版本踩到并已记录的问题

1. 新建知识库时 RAPTOR 与 GraphRAG 默认开启，解析负载成倍增加。
2. Elasticsearch 未设堆上限时按容器内存自动放大到 4.4 GB。
3. 思考型生成模型在 RAGFlow 中会出现空回答。
4. RAGFlow 会追加自己的引用规则，覆盖助手提示词中的格式要求。
5. 按阅读顺序重转正文只在「正文被表格分割」的文件上有效，不是通用解。
6. 重建容器必须使用部署专用的密码文件，否则连不上 MySQL。
7. 正文库与目录库的相似度阈值需要分别标定。
