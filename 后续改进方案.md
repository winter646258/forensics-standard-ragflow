# 后续改进方案

> 日期：2026-09-11
> 依据：本仓库方案书 v0.4 与 `ragflow-import/09-检索验收记录.md` 的实测结果

本文件列出下一阶段要解决的问题，按优先级排列。每条包含现状证据、目标、
具体做法与验收标准。优先级顺序的依据是：越靠前的越影响“回答可回溯”这一核心承诺，
或越是其他工作的前置条件。

---

## 优先级 1：引用格式收敛

**现状**：同一个问题「电子数据检验鉴定是否应当以副本作为操作对象」，
一次回答给出 `SF/Z JD0400001-2014 第 5.2.2.1 条`，另一次只给出
`[ID:0][ID:1][ID:4]`。结论都对，但依据的可读性与可核对性不稳定。

**原因**：RAGFlow 在交给模型的知识片段里带 `[ID:x]` 标记，模型有时直接复述标记；
当前提示词只要求“必须标注标准编号 + 条款号”，没有禁止输出内部标记。

**做法**

1. 提示词增加硬约束：禁止出现 `[ID:` 形式；引用统一写成「标准编号 第 X 条」。
2. 检索参数保证每条引用都能落到具体切片，避免模型只能拿到整章内容而放弃标注。
3. 固化回归用例：同一问题连续提问 5 次，统计引用合规率。

**验收**：回归用例连续 5 次，每次回答均含标准编号与条款号，且不出现 `[ID:`。

---

## 优先级 2：建立标准卡片索引，解锁场景匹配验收

**现状**：验收用例集 AC-01～AC-24 期望命中的是 GA/T、GB/T 标准，
但知识库目前只有 2 份 SF/Z 正文，且没有任何元数据索引，
因此这套用例无法按设计判定——不是系统答错，而是判定所需的数据层不存在。

**做法**

1. 新建独立知识库，只放标准元数据卡片
   （来源：`ragflow-import/02-元数据卡片.jsonl` 与 `06-标准总目录候选.jsonl`），
   与正文库物理分离，避免目录信息混进条款检索结果。
2. 助手同时绑定两个知识库，并在提示词中区分能力边界：
   正文库可给条款号；目录库只能给标准编号、名称、状态、适用范围摘要与官方链接。
3. 按验收用例需要补齐标签字段（阶段、对象、技术动作、场景）。

**验收**：AC-01～AC-16 人工评审通过率 ≥ 90%；
仅由目录库命中的推荐不得输出任何条款号。

---

## 优先级 3：批量导入其余 13 份已取得正文

**现状**：13 份正文已取得但未入库；已入库的 2 份里，有 1 份的原始转换物
需要按阅读顺序重转才能用。

**做法**

1. 导入前门禁：先跑 `validate_import.py`（哈希与结构），
   再做切片质量抽检（条款是否成句、切片数是否与文档规模匹配）。
2. 对“切片数异常偏少”的文件自动改用 `convert_pdf_reading_order.py` 重转。
3. 为 SiliconFlow 的偶发中断加导入重试，失败项写入导入日志，不做静默跳过。

**验收**：13 份全部解析完成；每份至少有一个条款级问题能被正确引用；
导入日志无未处理失败项。

---

## 优先级 4：检索参数调优

**现状**：`similarity_threshold=0.2`、`vector_similarity_weight=0.3`、`top_n=8`
均为默认值，未针对中文法条文本调整，也缺少量化依据。

**做法**：用 AC 用例集做小规模参数扫描
（阈值 0.15/0.20/0.25，向量权重 0.3/0.5/0.7），
记录命中率、误召回与首条命中是否落在正确条款。

**验收**：产出一张参数—命中率对照表，选定一组写回助手配置，
并说明选择理由与适用边界。

---

## 优先级 5：环境稳定性与自动化恢复

**现状**：Docker Desktop 引擎在这台机器上会周期性返回 500/502，
容器内 DNS 曾出现秒级反复失败，重建容器必须带 `.env.local`，
否则密码与数据卷不一致会导致 RAGFlow 无法连接 MySQL。

**做法**

1. 保留并完善 `ragflow-deploy/start-docker.ps1`：
   健康判断用容器列表而非版本号（已完成），增加恢复失败时的明确提示。
2. 保留 RAGFlow 容器的固定 DNS 配置，绕开不稳定的内部转发器（已完成）。
3. 建立知识库备份流程：定期导出 MySQL 数据与 MinIO 对象，
   确保数据卷损坏时可以重建而不必重新导入全部标准。

**验收**：连续 24 小时无人工干预可用；引擎异常后恢复脚本一次执行成功；
完成一次备份与恢复演练。

---

## 优先级 6：验收自动化

**现状**：检索与端到端验收目前靠临时脚本执行、人工判读结论。

**做法**：把 `09-检索验收记录.md` 中的用例固化为脚本，
自动完成提问、抓取回答，并按规则校验：
是否包含标准编号与条款号、是否出现 `[ID:`、是否引用了知识库中不存在的编号、
证据不足类问题是否按要求拒答。

**验收**：一条命令产出验收报告，含逐用例结论与总通过率。

---

## 优先级 7：仓库与合规策略

**现状**：仓库当前为私有；标准正文与切片明细
（`03-正文切片关联.jsonl`，其中 `section` 字段含条款原文）均未上传；
元数据卡片与哈希台账记录了本机路径。

**做法**

1. 维持“公开部分 = 部署与管理方案”的边界；
   若转为公开，先确认标准文本的转载授权，并处理记录中的本机路径。
2. API 令牌不写入脚本与仓库；记录令牌轮换与撤销流程。
3. 在 README 中持续维护内容边界说明。

**验收**：公开可见范围内不含标准正文、切片明细与任何凭据；
本机路径信息在转为公开前完成占位化处理。

---

## 建议的执行顺序

先做优先级 1（直接决定回答能不能用），再做优先级 2（解锁既定的验收用例集），
然后按 3 → 4 → 5 → 6 推进，优先级 7 在第 3 步批量导入完成后一并处理。

其中优先级 2 与 3 可以并行：前者补数据层，后者补正文规模，
两者都完成后才能跑出有意义的场景匹配验收结果。
