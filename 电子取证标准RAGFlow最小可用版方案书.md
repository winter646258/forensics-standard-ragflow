# 电子取证标准 RAGFlow 最小可用版方案书

> 版本：v0.4
> 日期：2026-09-11
> 项目状态：端到端问答已跑通；两份试运行标准均完成正文重建与解析；待补标准卡片以支撑场景匹配验收

## 1. 项目目标

建设一个面向电子取证国家标准、公安行业标准和司法鉴定技术规范的本地知识库。用户通过 RAGFlow 网页输入问题，系统从已核验的标准正文中检索相关内容，并返回带标准编号、来源文件和条款位置的依据。

本阶段追求“可检索、可回溯、可维护”的最小可用版本，不追求复杂的智能体或业务系统集成。

## 2. 本阶段范围

### 2.1 包含内容

- 使用 RAGFlow 自带网页作为唯一使用入口。
- 建立一个知识库：`电子取证国家及行业标准`。
- 导入已取得且完成基础质量检查的标准正文。
- 为每份标准保留标准编号、名称、版本、状态、来源和哈希。
- 对正文进行切片、向量化、全文检索和重排序。
- 用固定验收问题检查条款召回、引用完整性和不确定性表达。
- 形成可放入 GitHub 的部署文件、导入清单、元数据模板和操作说明。

### 2.2 明确不包含

- 不建设自主 Agent。
- 不开发独立前端、移动端或业务系统接口。
- 不自动作出法律有效性或案件结论。
- 不在缺少权威正文时编造条款号、条款原文或强制性要求。
- 不把未核验、废止或已被替代的标准作为主推荐依据。

## 3. 目标使用方式

```text
打开 RAGFlow 网页
    ↓
进入“电子取证国家及行业标准”知识库
    ↓
输入问题
    ↓
检索相关标准正文切片
    ↓
按相关性重排序
    ↓
生成带来源和条款定位的回答
```

示例问题：

> 现场获取电子数据有哪些基本要求？依据哪个标准和条款？

合格结果至少应包含：标准编号、标准名称、匹配理由、正文证据状态、条款或章节定位、来源文件。正文尚未取得时，只能返回标准元数据和官方来源，不能输出具体条款。

## 4. 总体架构

```text
用户浏览器
    ↓ http://127.0.0.1:8081
RAGFlow
    ├─ 文件解析与切片
    ├─ 知识库和检索测试
    ├─ 混合检索、向量检索、重排序
    └─ 基于检索结果生成回答
         ↓
MySQL       业务数据、用户、文件和知识库状态
Elasticsearch 全文索引和向量检索
MinIO       原始文件和处理文件
Redis/Valkey 任务队列和缓存
         ↓
硅基流动 API
    ├─ LLM：Qwen/Qwen3.5-9B
    ├─ Embedding：Pro/BAAI/bge-m3
    └─ Rerank：Pro/BAAI/bge-reranker-v2-m3
```

RAGFlow 是知识库和 RAG 平台，不等于 Agent。当前项目只使用它的知识库、解析和检索问答功能。

## 5. 模型与 API 方案

RAGFlow 本身部署在本机 Docker 中，但当前使用的云端模型需要调用硅基流动 API：

- Embedding 把标准条款转换为向量。
- Rerank 对召回的条款重新排序。
- LLM 根据检索到的条款组织回答。

API Key 只在本机 RAGFlow 模型设置中保存，不写入 GitHub、Markdown、脚本或 `.env.example`。项目不开发额外 API；以后如需接入案件系统，再单独设计接口。

## 6. 数据分层与状态

### 6.1 标准数据层

每份标准至少关联以下字段：

`standard_id`、`standard_number`、`title`、`version`、`status`、`body_status`、`source_url`、`source_evidence`、`sha256_pdf`、`sha256_markdown`、`review_status`。

其中：

- `status` 表示现行、废止或被替代等标准状态。
- `body_status` 区分正文已获取、仅有官方目录、待获取和来源待核验。
- `review_status` 区分未复核、人工复核和可导入。
- 只有正文已获取且来源可靠的条目才能提供条款级回答。

### 6.2 导入产物

- `01-正文Markdown/`：供 RAGFlow 解析的正文。
- `02-元数据卡片/`：逐标准元数据卡片。
- `02-元数据卡片.jsonl`：批量索引。
- `03-正文切片关联.jsonl`：切片与标准、章节、源文件的关联。
- `04-哈希台账.jsonl`：原始 PDF 和 Markdown 的大小、哈希与来源记录。
- `06-标准总目录候选.jsonl`：目录候选，不直接作为正文知识上传。

## 7. 当前数据基础

已有材料包括：

- 已整理的 GB/T、GA/T、SF/Z、SF/T 标准目录和官方核验记录。
- 15 份 SF/Z 正文 PDF 及其 Markdown 转换物。
- 15 份文件的基础转换质量报告和哈希台账。
- 元数据卡片、正文切片关联和 RAGFlow 导入清单。
- 已创建的 RAGFlow 知识库：`电子取证国家及行业标准`。

转换质量抽查结论为：15/15 文件存在，原始 PDF 哈希与台账一致；转换物没有发现空白正文或文件级截断证据，但表格线性化和部分低文本页仍需在条款级使用时回看 PDF 原页。

4 份已被 SF/T 2023 标准替代的旧规范保留作版本追溯，默认不作为主推荐。标准编号和名称存在冲突的文件不得直接导入，必须先核对官方目录和正文首页。

## 8. 实施步骤

### 阶段一：恢复运行环境

1. 恢复 Docker Desktop 和 WSL。
2. 确认 RAGFlow、MySQL、Elasticsearch、MinIO、Redis/Valkey 正常运行。
3. 确认 `http://127.0.0.1:8081` 可访问。
4. 保留 Docker 临时目录备份，不触碰 `D:\RAGFlow\docker-data` 数据卷。

### 阶段二：小规模导入

1. 确认两份试运行标准的解析状态。
2. 检查 chunks 数量、文件状态和解析日志。
3. 对条款边界、标题、编号和表格内容进行抽查。
4. 将有问题的文件从主知识库中暂缓，保留在追溯区。

### 阶段三：检索验收

使用已有验收用例，至少覆盖：

- 现场获取电子数据。
- 手机数据提取和检验。
- 删除数据恢复。
- 文件一致性和哈希校验。
- 数据库真实性。
- 信息不足时不强行推荐。
- 正文缺失时不虚构条款。

通过标准：主推荐对象和动作匹配；现行标准优先；正文缺失不输出条款；结果显示来源和证据状态；基础案例人工评审通过率达到 90% 以上。

### 阶段四：扩充知识库

首轮验收通过后，再分批导入其余已取得且合法可用的正文。仅有目录和元数据的标准可以作为候选索引，但不能混入正文检索结果。

## 9. 最终交付形态

### 9.1 本地可用成果

- Docker 启动的 RAGFlow 服务。
- 一个电子取证标准知识库。
- 可通过网页进行标准问答和检索测试。
- 结果包含标准来源、版本状态和条款定位。
- 标准正文、元数据、切片关联和哈希台账相互对应。

### 9.2 GitHub 项目成果

适合上传：

- Docker Compose 覆盖配置。
- `.env.example` 和部署说明。
- 导入、校验和哈希脚本。
- `ragflow-deploy/compose.ps1` 启停入口和 `ragflow-import/validate_import.py` 资产校验器。
- 不含真实凭据的 `.env.example`、`.gitignore` 和部署说明。
- 标准目录、元数据模板、切片关联格式。
- 脱敏后的示例文件和验收用例。
- README、许可说明和使用边界。

不上传：

- API Key 和数据库密码。
- Docker 数据目录和数据库卷。
- 未确认有公开转载权限的标准全文。
- 个人信息、内部案件材料和来源不明文件。

公共 GitHub 仓库应主要保存“部署与管理方案”；标准全文和完整索引数据可留在本地或私有仓库。

## 10. 风险与限制

- 标准元数据已核验不等于标准正文已取得。
- PDF 转 Markdown 后的表格、跨页条款和页眉页脚需要重点抽查。
- RAG 检索结果不能替代鉴定人、办案人员或法律专业人员判断。
- 硅基流动 API 受账户额度、可用模型和网络状态影响。
- 本机 Docker 当前资源偏紧，批量解析前应确认内存和磁盘空间。
- 标准全文的保存、传播和公开上传必须符合来源授权和版权要求。

## 11. 后续工作基线

后续工作严格按以下优先级推进：

1. 恢复 Docker Desktop/CLI，并确认 Docker 可用内存达到 RAGFlow 官方最低要求。
2. 使用 `ragflow-import/validate_import.py --pdf-root` 完成源 PDF 大小和 SHA-256 复核。
3. 把标准目录/元数据卡片建为独立索引，使场景匹配类验收用例（AC-01～AC-24）可判定。
4. 收敛回答中的引用格式，去掉 `[ID:x]` 与标准编号混排。
5. 分批扩展其余 13 份已取得正文，导入前统一走重转或清洗流程。
6. 整理不含密钥和受限正文的 GitHub 项目包。
7. 暂不加入 Agent、独立前端和业务接口。

## 12. 2026-09-10 执行记录

- 已确认官方 Compose 源目录存在：`D:\RAGFlow\ragflow\docker`，版本配置为 `infiniflow/ragflow:v0.27.1`。
- 已落地本地覆盖和 PowerShell 启停入口：`ragflow-deploy/docker-compose.override.yml`、`ragflow-deploy/compose.ps1`。
- 已落地不依赖第三方 Python 包的导入资产校验器：`ragflow-import/validate_import.py`。
- 校验结果：15/15 份元数据、63 个正文切片、11 个主推荐、4 个替代旧规范；15 份 Markdown 哈希全部一致，未发现结构性失败。
- 当前阻塞：系统找不到 Docker CLI，也未发现 Docker Desktop 可执行文件或运行进程，因此尚未启动容器、上传文件或执行 RAGFlow 网页检索。
- 本轮未修改 `D:\RAGFlow\docker-data` 或其他数据卷，也未把标准全文加入公共 Git 文件边界。

## 13. 2026-09-11 执行记录

### 环境恢复

- Docker Desktop 实际安装在用户目录 `%LOCALAPPDATA%\Programs\DockerDesktop`，
  并非 `C:\Program Files\Docker`；此前按后者查找才误判为未安装。
- 反复启动失败的根因是残留套接字：后台进程被强制结束后，
  `AppData\Local\Docker\run\sailor-ingest.sock` 与
  `AppData\Local\docker-secrets-engine\engine.sock` 变成无法重命名的悬空符号链接，
  下次启动时 Secrets Engine / Ingest server 初始化即崩溃。
- 处理方式：结束全部 Docker Desktop 进程、`wsl --shutdown`、
  把残留 `run` 与 `docker-secrets-engine` 目录改名归档后重新启动。
  该流程已固化为 `ragflow-deploy/start-docker.ps1`。
- 恢复后 Docker Desktop 4.90.0 / Engine 29.7.2 正常，
  RAGFlow、MySQL、Elasticsearch、MinIO、Valkey 五个容器全部运行，
  `http://127.0.0.1:8081` 返回 200。

### 解析与检索

- `SF/Z JD0400001-2014` 解析完成，61 个条款级切片；
  `SF/Z JD0400002-2015` 解析完成，但只有 2 个切片。
- 已用 `ragflow-import/clean_markdown.py` 生成清洗版正文
  （仅删目录点线、重复页眉、页码），两份试运行标准改为导入清洗版，
  未清洗的重复文档已从知识库删除，原始文件仍保留在 `01-正文Markdown`。
- 检索层验收结果见 `ragflow-import/09-检索验收记录.md`。
  第一份标准可精确召回 5.2.2.1、4.3～4.6 等具体条款；
  第二份标准因转换缺陷只能返回整篇概要。

### 本轮明确的未完成事项

- 端到端问答未验证：知识库尚未配置对话助手，
  因此“回答中是否规范标注标准编号与证据状态”没有实测证据。
- `SF/Z JD0400002-2015` 需重新转换后才能作为条款依据。
- 其余 13 份已取得正文尚未导入。
- 硅基流动接口存在偶发中断，批量导入前需要重试或错峰策略。

## 14. 2026-09-11 端到端问答与正文重建

### 对话助手

- 在知识库上创建助手「电子取证标准问答」，绑定该知识库并启用重排模型。
- 知识库语言由 `English` 修正为 `Chinese`。
- 提示词固定四条约束：必须标注标准编号与条款号；证据不足直接说明；
  已被代替的标准只作版本追溯；不作法律效力结论。
- 生成模型由方案原定的 `Qwen/Qwen3.5-9B` 调整为
  `Qwen/Qwen3-30B-A3B-Instruct-2507`。原因是前者为思考型模型，
  会把输出额度消耗在 `reasoning_content` 上，而 RAGFlow 只读取 `content`，
  实测出现空回答；RAGFlow 虽有针对 qwen3 的 `enable_thinking=false` 逻辑，
  但经 SiliconFlow 链路未生效。
- 端到端结果：副本与完整性校验、鉴定原则、现场静态获取、现场动态获取四类问题
  均给出正确条款依据；来源不明文件与法律效力判断两类问题正确回答“证据不足”。
- 引用格式收敛：RAGFlow 会在系统提示词之后追加 `citation_prompt`，
   要求用 `[ID:i]` 标注来源，模型因此常常只给标记而不写标准编号。
   已关闭助手的 `quote` 选项去掉该追加规则，并在提示词中要求
   每条结论写出「标准编号 + 条款号」、末尾输出「依据」小节
   （标准编号、条款号、来源文件名）。同一问题连续 5 次达到 5/5 合规；
   代价是失去 RAGFlow 内置的可点击引用面板。

### 第二份标准的正文重建

`SF/Z JD0400002-2015` 原转换物把正文当成表格处理，条款被打散到单元格里，
只能切出 2 个切片。实测确认问题出在按版式转换的工具，而非 PDF 本身：
该 PDF 有文本层，按阅读顺序提取可完整保留条款结构。

新增 `ragflow-import/convert_pdf_reading_order.py`：按阅读顺序提取文本层，
去除页眉页脚与目录点线，再重建章节号、条款号和列表层级。
重建结果写入 `01-正文Markdown-重转/`，由 `clean_markdown.py` 原样带入
`01-正文Markdown-清洗版/`，避免被机械清洗覆盖。

重建后该标准切片数由 2 个增加到 9 个，条款 3.1～3.4、5.1～5.5、
5.4.1～5.4.3 与 a)～f) 列表全部就位，
端到端回答可以正确引用 5.3、5.4。

### 本轮暴露的环境问题与处置

| 问题 | 处置 |
| --- | --- |
| Docker 引擎周期性返回 500/502、容器 DNS 数秒内反复失败 | RAGFlow 容器 DNS 指向公共解析服务，绕开不稳定的内部转发器 `192.168.65.7` |
| 重建容器后 RAGFlow 无法连接 MySQL | 部署使用 `.env.local` 中的专用随机密码，重建必须带 `--env-file`；`compose.ps1` 已改为默认加载 |
| 恢复脚本把“部分可用”误判为健康 | `start-docker.ps1` 改用容器列表而非版本号判断引擎状态 |
